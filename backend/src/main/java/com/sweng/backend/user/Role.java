package com.sweng.backend.user;

/** Enumeration of user roles in the system. */
public enum Role {
  /** Regular customer role. */
  CUSTOMER,
  /** Restaurant owner role with management privileges. */
  RESTAURANT_OWNER,
  /** Administrator role with full access. */
  ADMIN
}
